{
  "name": "receipt-drop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/v0-preview-server.js",
    "vercel:dev": "npx vercel dev",
    "build": "echo static site",
    "check": "node --check api/submit.js && node --check api/poll.js && node --check api/ack.js && node --check api/fail.js && node --check api/heartbeat.js && node --check api/status.js && node --check api/archive-status.js && node --check api/admin.js && node --check public/app.js && node --check scripts/v0-preview-server.js",
    "sync:archive": "bash scripts/sync-receipt-archive-from-mbp.sh"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.3",
    "@vercel/speed-insights": "^2.0.0"
  }
}
